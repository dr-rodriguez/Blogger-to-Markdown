[project]
name = "blogger-to-markdown"
version = "0.1.0"
description = "Scripts to convert Blogger XML output (atom feed) to Markdown"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
